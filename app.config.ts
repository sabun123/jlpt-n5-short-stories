import { defineAppConfig } from "#imports";

export default defineAppConfig({
  ui: {
    primary: "green",
    notifications: {
      position: "top-right",
    },
    icons: {
      dynamic: true,
    },
  },
});
